# ── JannAI Environment Variables ──

# Google Gemini API key (Flash model family)
# If not set, all AI calls will use clearly-labeled mock responses
GEMINI_API_KEY=

# Backend server port (default: 3001)
PORT=3001

# SQLite database file path (default: ./db/jannai.db)
DATABASE_PATH=./db/jannai.db
